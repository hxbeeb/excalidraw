apiVersion: v1
kind: Secret
metadata:
  name: excalidraw-db
type: Opaque
stringData:
  # Replace with the PostgreSQL connection string. Do not commit real credentials.
  DATABASE_URL: postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
